import React from 'react'

const NoWalletAddress = (props)=>{

    return(

        <section>

            <h3 className="alert-warning">{props.warning}</h3>

        </section>
    )    

}

export default NoWalletAddress